{
  "name": "irc-ebook-downloader",
  "version": "0.1.0",
  "description": "TUI for downloading ebooks from IRC (#ebooks on irchighway)",
  "type": "commonjs",
  "license": "MIT",
  "bin": { "ebook-dl": "index.js" },
  "scripts": { "start": "node index.js" },
  "engines": { "node": ">=16" },
  "pkg": {
    "//": "blessed loads these at runtime via a computed require()/path, so pkg's static analysis misses them — force-include or the binary crashes with 'Cannot find module ./widgets/list'.",
    "assets": [
      "node_modules/blessed/lib/widgets/*.js",
      "node_modules/blessed/usr/**/*"
    ]
  },
  "dependencies": {
    "adm-zip": "^0.5.10",
    "blessed": "^0.1.81",
    "irc-framework": "^4.13.1"
  }
}
